{
  "name": "vibe-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "nodemon --legacy-watch --exec ts-node --transpile-only index.ts",
    "build": "npm install && tsc"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2"
  }
}
